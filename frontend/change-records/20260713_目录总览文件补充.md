# 前端目录总览文件补充

## 修改时间

2026-07-13 17:28:37 CST

## 修改原因

需要在前端目录内提供一个总览文件，使开发者和大模型能够快速确认当前目录结构、每个子目录的职责和代码放置规则。

## 修改范围

- 新增 `frontend/README.md`；
- 记录前端完整目录树；
- 说明每个目录的内容与职责；
- 增加目录总览的同步维护规则。

## 涉及文件

- `frontend/README.md`；
- `frontend/change-records/20260713_目录总览文件补充.md`；
- 根目录 `README.md`；
- 根目录 `AGENTS.md`；
- `docs/技术选型说明.md`；
- `docs/迭代流程记录.md`。

## 实际修改

- 为前端目录增加统一入口说明；
- 记录 `app`、`features`、`components`、`services` 等目录的职责；
- 明确新增、删除、移动或重命名目录时必须更新总览；
- 在总览中列出当前前端修改记录。
- 同步更新技术选型说明中的前端目录树。

## 逻辑变化

无业务逻辑变化。新增的是前端目录结构的可追踪规则。

## 数据库或缓存变化

无。

## 验证结果

- 已检查 `frontend/README.md` 中的目录树与实际目录一致；
- 已检查修改记录命名符合规则；
- 本次没有前端代码测试。

## 已知风险与后续事项

- 初始化 Next.js 后需要及时补充生成的关键配置文件；
- 后续目录变化如果没有同步更新 README，会导致总览失真，因此已将规则写入 `AGENTS.md`。
